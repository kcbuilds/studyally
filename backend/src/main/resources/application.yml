server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: business.studyally@gmail.com
    password: ccco rleu rmfy caub
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

  jackson:
    serialization:
      fail-on-empty-beans: false

app:
  jwt:
    secret: studybuddy-super-secret-jwt-key-change-this
    expiration-ms: 86400000

logging:
  level:
    com.studybuddy: DEBUG
    org.springframework.security: INFO
